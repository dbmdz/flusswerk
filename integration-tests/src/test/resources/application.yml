flusswerk:
  processing:
    threads: 1
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  routing:
    exchange: my.exchange
    readFrom:
      - first
      - second
    writeTo: target.queue
    failure policies:
      first:
          retries: 5
          backoffMs: 500