spring:
  application:
    name: integration-tests
  profiles:
    active: local

flusswerk:
  processing:
    threads: 2
  rabbitmq:
    hosts:
      - localhost
    username: guest
    password: guest
  routing:
    exchange: my.exchange
    incoming:
      - input.first
      - input.second
    outgoing:
      default: output
    failure policies:
      "[input.first]":
          retries: 5
          backoff: 50ms

---

spring:
  profiles: local

flusswerk:
  rabbitmq:
    hosts:
      - localhost

---
spring:
  profiles: ci

flusswerk:
  rabbitmq:
    hosts:
      - docker
