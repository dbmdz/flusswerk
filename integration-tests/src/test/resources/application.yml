spring:
  application:
    name: integration-tests
  profiles:
    active: local

flusswerk:
  processing:
    threads: 1
  rabbitmq:
    hosts:
      - localhost
    username: guest
    password: guest
  routing:
    exchange: my.exchange
    incoming:
      - first
      - second
    outgoing:
      default: target.queue
    failure policies:
      first:
          retries: 5
          backoff: 500ms

---

spring:
  profiles: local

flusswerk:
  rabbitmq:
    host: localhost

---
spring:
  profiles: ci

flusswerk:
  rabbitmq:
    host: docker